export async function getSyncDataSummary() {
  // Placeholder pour connexion future au repo SYNC
  return "Résumé interne Sync (segments, types de clients, exemples de soumissions) disponible côté système.";
}
